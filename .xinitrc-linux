#!/bin/bash

if [ -z "$NO_WM" ]; then
    export GDK_BACKEND=X11
    exec gnome-session
	# exec startkde
else
    # dimensions for old laptop were 142 47
	exec alacritty --dimensions $COLUMNS $LINES -e fish -c "t"
fi
