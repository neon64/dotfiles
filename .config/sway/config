### Variables
#
# use super as the modifier key -
# warning - on most keyboards nothing maps to super by default so you will
# be unable to exit or launch anything!
set $mod Mod4

# preferred terminal emulator
set $term alacritty
set $window_switcher ~/.config/wofi/windows.py

# preferred web browser
set $browser firefox
set $files nautilus

# preferred application launcher
set $menu wofi
set $menu_toggle ~/.config/sway/toggle_wofi.sh

# movement keys
set $up l
set $down k
set $left j
set $right semicolon

# ricing
set $borderwidth 0

### Output configuration

exec ~/.azotebg

# more specific configuration at runtime
exec_always ~/.config/sway/configure.sh

# notification daemon
exec mako
# dynamic output management
exec systemd-cat -t kanshi kanshi
# session restore
exec ~/code/sway-session/sway-session restore && ~/code/sway-session/sway-session save-loop 240
# authentication popups
exec /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
# kde connect daemon
exec /usr/lib/kdeconnectd

# input mappings / libinput config
include input

# key/mouse/lid switch bindings
include bindings

# colors and fonts
include rice

# app-specific settings and overrides
include apps

# includes, amongst other things, a fix for DISPLAY and WAYLAND_DISPLAY env-vars on Arch Linux
include /etc/sway/config.d/*